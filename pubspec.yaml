name: chess_clock
description: A FIDE-regulated chess clock app.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.2.0
  flutter_svg: ^2.0.10+1
  flutter_glow: ^0.3.0
  neon_widgets: ^2.0.0
  confetti: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/mixkit-handgun-click-1660.mp3
